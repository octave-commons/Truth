
Can we centralize position based clustering into a distinct centralized thing based on position? We are going to use it *A LOT* We should at least pull the data structures for spatial clustering into protocols, or multi methods, depends... we will need this kind of logic a lot. most of it over the same positional grid... It's an oct tree, should be fine to split that off so everything with a position also has a "oct-block-id" and that gives it access to traverse the data structure up or ddown?
