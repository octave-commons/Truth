;; Questions ledger — append-only, numbered. Mirrors receipts.edn shape.
;; Keys: :id (monotonic int) :ts :origin :owner :audience (:user|:exploration)
;;       :status (:open|:answered|:superseded) :question :context :refs [answer-ids|files]
;; Never edit or delete past lines. Answers live in ledger/answers.edn keyed by :answers-q.
{:id 1 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :user :status :open
 :question "The formation-survival trio is a ~2500-line uncommitted diff across 45 files on branch spark-gravity-bound-body (HEAD = spark card 4). Commit + verify it before building further, or keep working on top of the dirty tree?"
 :context "Working tree touches integrator, sink, disc-evolution, classifier, kepler, gravity/barnes-hut, +tests. Concurrent opencode agent may own this dirt. Fork-tax guidance forbids blanket staging."
 :refs []}
{:id 2 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :user :status :open
 :question "Which thread is the priority next step: (a) direct-merge L renormalization (finish the sink fix), (b) 1024 post-birth stripping autopsy, or (c) pause coding and fly the live pm2 window to observe the new physics?"
 :context "Intro offered these three. (a) is small + finishes card sink-absorb-angular-momentum-renormalization. (b) informs a survival gate. (c) is verification, not code."
 :refs []}
{:id 3 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :user :status :open
 :question "How autonomous should the sonnet subagents be: research/investigate + write findings only, or allowed to modify src/ and test/ and commit?"
 :context "User asked for medium-effort sonnet subagents that ask questions relentlessly. Physics changes are high-blast-radius per the blocker card."
 :refs []}
{:id 4 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :user :status :open
 :question "Is the north star still 'produce >=1 c/planet-candidate under normal sim so the binding->commitment->voxel loop becomes reachable'? Survival rate is 1/25, up from 0. Do we push for candidate emergence, or is 1/25 enough to switch to the player-facing loop?"
 :context "planet-orbit-circularization-blocker is the acknowledged upstream gate for the whole spark-flight/voxel roadmap."
 :refs []}
{:id 5 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :exploration :status :answered
 :question "Does the full test suite pass with the uncommitted formation-survival diff applied?"
 :context "Verifying the dirty tree before deciding to build on it."
 :refs [1]}
{:id 6 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :exploration :status :open
 :question "What/where is the 'direct-merge packet channel' that keeps raw capture-radius L, and does it feed absorb-packet or a separate sink path? The sink-absorb card cites sink.clj:136 for whole-parcel absorbs — is the t~4100 kAU population from that path or another?"
 :context "Intro thread (a). sink-absorb-angular-momentum-renormalization card is in review; card 1 renormalized only disk-route packets."
 :refs []}
{:id 7 :ts "2026-07-24T00:00:00Z" :origin "session review" :owner "claude" :audience :exploration :status :open
 :question "What is the velocity/energy timeline of body 1024 from bound 32.4-AU birth to stripping within 500 ticks? Is the stripper the identified sibling star, and is it a WH-split close-encounter degradation (design §4 note) or a real physical tide?"
 :context "Intro thread (b). Probe data in scratchpad/trio.edn; autopsy likely informs a survival gate."
 :refs []}
{:id 100 :ts "2026-07-24T00:00:00Z" :origin "thread (a) sink absorb L renorm review" :owner "claude" :audience :exploration :status :open
 :question "Does merge-route raw bulk angular momentum in sink.clj absorb-packet leak into disc/disk-radius derivation and birth the kAU disks seen at t~4100 in scratchpad/trio.edn?"
 :context "kanban/tasks/sink-absorb-angular-momentum-renormalization.md hypothesizes this as the mechanism. Investigating before implementing any further fix."
 :refs []}
{:id 103 :ts "2026-07-24T00:00:00Z" :origin "thread (a) sink absorb L renorm review" :owner "claude" :audience :exploration :status :open
 :question "Is condensation-seeder-system's disc/in-disc? gate (fed by disc-identification-system's most-massive-star reference) the actual mechanism placing :planetesimal bodies at kAU distances at t~4100, rather than the sink.clj absorb-packet L that the card names?"
 :context "Static trace strongly implicates disc.clj:62-92 (still most-massive, unlike classifier/dominant-attractor which the sibling card central-star-nearest-attractor.md landed only for classifier.clj). Runtime probe (scratchpad/probe3.clj-equivalent inline script) pending to confirm disc-tag values for the actual far-flung bodies."
 :refs []}
{:id 104 :ts "2026-07-24T00:00:00Z" :origin "thread (a) sink absorb L renorm review" :owner "claude" :audience :user :status :open
 :question "The global nebula spin parameter (default 0.6, bootstrap.clj default-world-options) imparts coherent large-scale rotation to the whole cloud. disc-classify's rotationally-supported heuristic (vt > 2|vr| AND h/r < 0.3 AND two-body-bound) has NO explicit distance/proximity gate to the reference star -- so even after fixing disc-identification-system to use the nearest BOUND star (classifier/dominant-attractor) instead of the world's most-massive star, a gas parcel kAU away that is (a) two-body-bound to SOME star (easy at large r for typical cold infall speeds, since the bound threshold v^2 <= 2GM/r loosens with r) and (b) sharing the cloud's coherent rigid-body-like rotation (so its velocity looks tangential relative to ANY reference point) could still pass the heuristic and get wrongly tagged :disc, regardless of which star is chosen as reference. Should disc-classify or disc-identification-system add an explicit proximity gate (e.g. tag :disc only within some multiple of the reference star's accretion/feeding radius or the existing max-fragmentation-disk-radius/disk-formation-radius scale), or is switching the reference star to the nearest bound one (mirroring the already-landed classifier fix) considered sufficient/in-scope for THIS card, with the coherent-rotation false-positive tracked as a separate follow-up?"
 :context "This is a design decision beyond the stated scope of sink-absorb-angular-momentum-renormalization.md (which only asked to mirror the disk-route L renormalization) and beyond central-star-nearest-attractor.md (which only touched classifier.clj). Only the human owner can decide whether to expand scope here or defer to a new card."
 :refs []}
{:id 8 :ts "2026-07-24T13:00:00Z" :origin "thread (a) probe pivot" :owner "claude" :audience :exploration :status :open
 :question "What spawn path creates :planet-state bodies (disc-tag nil, dominant-attractor nil) at 1897-71441 AU by tick 4100? Candidates: disc-evolution GI/binary fragmentation branches, planet_formation/orbit core-accretion seeds, or a :planetesimal->:planet reclassification after kAU placement. Did formation-placement-v2's disk-scale gate + Hill clamp actually fire on this path, or does it not cover it?"
 :context "Refutes both prior hypotheses (A101 sink merge-L, A102 disc-seeder). Bodies are :planet not :planetesimal, untagged. Need the real emitter with file:line + probe confirmation."
 :refs []}
{:id 9 :ts "2026-07-24T13:20:00Z" :origin "probe6/probe4 fling finding" :owner "claude" :audience :exploration :status :open
 :question "Why are the 12 :planet bodies (born ~35 AU, tick ~4040) flung to kAU by tick 4100? Are they in the substep-matter-states population that universal-compact-substepping guards? Is the dominance gate failing and the K total-force sub-step (a) not firing, (b) firing but still flinging, or (c) are these bodies routed to raw Euler? Instrument kinematics-cell for these eids over ticks 4040-4100: log per-tick {dominance-gate pass/fail, K, |dv|, integrator-path}."
 :context "Reframes the whole investigation from placement to post-birth ejection. Files: src/domain/integrator/kinematics.clj (compact-advance/euler-advance gate), substep population membership, dominance ratio."
 :refs []}
{:id 200 :ts "2026-07-24T14:00:00Z" :origin "fling-mechanism investigation" :owner "claude" :audience :exploration :status :open
 :question "Is the spawn-seam stale-anchor bug (planet-seeds specs baked against the star's PRE-tick position, materialized into the POST-step-physics world where the star has already moved) the mechanism for the 12 kAU planets? Instrument the exact spawn tick and compare spec position/velocity to the star's displacement over that same tick."
 :context "Reframes q9/A7 (post-birth integrator fling) entirely: the velocity anomaly is present the FIRST tick the body is observed, not accumulated. See A200 for the full proof chain (probe_fling2-5.clj)."
 :refs [9]}
{:id 201 :ts "2026-07-24T14:15:00Z" :origin "fling-mechanism investigation" :owner "claude" :audience :exploration :status :open
 :question "Does resolving spawn specs against the parent's CURRENT (post-step-physics) position/velocity via a generic :spawn-parent/:rel-position/:rel-velocity contract in build-planet-spec + bootstrap/spawn-entity fix the spawn-seam staleness without breaking any existing spawn path (disk fragments, condensation seeds, etc. that do not set :spawn-parent)?"
 :context "Design choice: mirror multi-timescale-integration.md §3.0's relative-coordinate fix, but at materialize-lifecycle time rather than inside the integrator, since that is the seam where the staleness is introduced."
 :refs [200]}
{:id 202 :ts "2026-07-24T14:30:00Z" :origin "fling-mechanism investigation" :owner "aaron (user)" :audience :user :status :open
 :question "The spawn-seam fix raises the seed-42 bound-planet count from 0/12 to 6/12 by tick 4100, but the 4 innermost and 2 outermost planets are still flung -- likely real dynamical scattering in the 4-star (491/258/507/73) formation-era system (their dominance-gate ratios were already 1-60, well under the 100x threshold, at spawn). Do you want a follow-up investigation into that residual scattering now (same session/card), or is 'went from 0 to 6 bound planets, spawn-seam bug fixed and proven' sufficient to close THIS card and track the residual scattering as a new card?"
 :context "kanban/tasks/universal-compact-substepping.md's 'done when' bar is >=1 bound planet surviving the formation era -- now met. Full elimination of the fling for all 12 needs its own investigation (possibly a separate close-encounter/hybrid-integration card per design §4's known limitation)."
 :refs [200 201 202]}
